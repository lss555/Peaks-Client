
<div class='peaks-list-container'>
  <h2>{{name}} </h2>
  {{#with story}}
  <ul id='peaks'>
    {{/with}}
    {{#each peaks as |peak|}}
    <li>
      {{peak.name}}, ID: {{peak._id}}
    </li>
  {{/each}}
  </ul>
</div>
